# Game Media Sync Decky Plugin

Decky Loader plugin for manually syncing Steam Deck screenshots and Steam game
recording clips to Immich.

Build from the repository root:

```bash
python scripts/package_decky.py
```

The resulting ZIP is written to `build/decky/game-media-sync-decky.zip` and can
be installed from Decky Loader's Developer settings.
